var evenNumbers = [];
var oddNumbers = [];
for (var i = 0; i <= 15; i++) {
    if (i % 2 == 0) {
        evenNumbers.push(i);
        console.log(evenNumbers);
    }
    else {
        oddNumbers.push(i);
        console.log(oddNumbers);
    }
}
